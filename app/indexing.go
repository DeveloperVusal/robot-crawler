package app

import (
	// "fmt"
	// "log"

	// "robot/core"

	_ "github.com/go-sql-driver/mysql"
)

type Indexing struct{}

func (indx *Indexing) Run(id uint64, url string, domain_id uint64, domain_full string) {

	// dbn.QueryRow(context.Background())
}
